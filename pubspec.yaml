name: new_features
description: New Features
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.3.4

dependencies:
  flutter:
    sdk: flutter
    
  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  intl: ^0.19.0
  unicons: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
